# คู่มือการใช้งาน Admin User - Estate Connect

## 📋 ข้อมูล Admin User

### ข้อมูลการเข้าสู่ระบบ
- **อีเมล**: `admin@example.com`
- **รหัสผ่าน**: `password`
- **Role**: `admin`

## 🚀 วิธีการใช้งาน

### 1. เข้าสู่ระบบ Admin Panel

1. เปิดเบราว์เซอร์และไปที่: `http://localhost/estateconnect/login`
2. กรอกข้อมูล:
   - **อีเมล**: `admin@example.com`
   - **รหัสผ่าน**: `password`
3. คลิก "เข้าสู่ระบบ"
4. ระบบจะ redirect ไปที่ Admin Dashboard อัตโนมัติ: `/admin/dashboard`

### 2. ฟีเจอร์ใน Admin Panel

#### 📊 Dashboard (`/admin/dashboard`)
- ภาพรวมระบบ
- สถิติทรัพย์สิน (รอตรวจสอบ, เผยแพร่แล้ว)
- สถิติ Leads
- สถิติ Scraping

#### 🏠 จัดการทรัพย์สิน (`/admin/properties`)
- ดูรายการทรัพย์สินทั้งหมด
- แก้ไขข้อมูลทรัพย์สิน
- Publish/Unpublish ทรัพย์สิน
- ลบทรัพย์สิน
- AI Rewrite คำอธิบาย

#### 👥 จัดการ Leads (`/admin/leads`)
- ดูรายการลูกค้าที่สนใจ
- Export ข้อมูลเป็น CSV
- ดูรายละเอียด Lead

#### 🔄 จัดการ Scraper (`/admin/scraper`)
- ดูสถานะการ Scrape
- รัน Scraper ใหม่

#### 👤 จัดการผู้ใช้ (`/admin/users`)
- ดูรายการผู้ใช้
- สร้างผู้ใช้ใหม่
- แก้ไขข้อมูลผู้ใช้
- ลบผู้ใช้

## 🔐 Role System

ระบบมี 3 ระดับสิทธิ์:

1. **Admin** - สิทธิ์เต็ม (จัดการทุกอย่าง)
2. **Editor** - แก้ไขและ Publish ทรัพย์สิน
3. **Viewer** - ดูข้อมูลเท่านั้น

## ⚙️ การสร้าง Admin User ใหม่

### วิธีที่ 1: ใช้ Seeder
```bash
php artisan db:seed --class=AdminUserSeeder
```

### วิธีที่ 2: สร้างผ่าน Admin Panel
1. เข้าสู่ระบบด้วย admin account
2. ไปที่ `/admin/users`
3. คลิก "สร้างผู้ใช้ใหม่"
4. กรอกข้อมูลและเลือก Role เป็น "admin"

### วิธีที่ 3: สร้างผ่าน Tinker
```bash
php artisan tinker
```
```php
$user = \App\Models\User::create([
    'name' => 'ชื่อผู้ใช้',
    'email' => 'email@example.com',
    'password' => \Hash::make('รหัสผ่าน'),
    'role' => 'admin',
]);
```

## 🔒 Security

- Admin routes ถูกป้องกันด้วย middleware `auth` และ `role:admin`
- ผู้ใช้ที่ไม่มี role `admin` จะไม่สามารถเข้าถึง admin panel ได้
- ควรเปลี่ยนรหัสผ่าน admin หลังจากใช้งานครั้งแรก

## 📝 หมายเหตุ

- Admin user ถูกสร้างอัตโนมัติเมื่อรัน migration และ seeder
- ควรเปลี่ยนรหัสผ่าน default (`password`) ทันทีหลังการติดตั้ง
- ระบบจะ redirect admin users ไปที่ `/admin/dashboard` อัตโนมัติหลัง login

## 🆘 Troubleshooting

### ไม่สามารถเข้าสู่ระบบได้
1. ตรวจสอบว่าได้รัน seeder แล้ว: `php artisan db:seed --class=AdminUserSeeder`
2. ตรวจสอบว่า user มี role เป็น `admin`
3. ลอง clear cache: `php artisan cache:clear`

### ไม่สามารถเข้าถึง Admin Panel
1. ตรวจสอบว่า user ที่ login มี role เป็น `admin`
2. ตรวจสอบว่า middleware ถูก register ใน `bootstrap/app.php`

